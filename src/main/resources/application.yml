spring:
  application:
    name: booking-system

  datasource:
    url: jdbc:postgresql://localhost:4001/booking_system
    username: abhilash
    password: abhi1234
    driver-class-name: org.postgresql.Driver
    hikari:
      data-source-properties:
        TimeZone: Asia/Kolkata
        
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 4000
